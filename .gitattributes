# Shell scripts must keep LF endings — CRLF makes Linux fail with
# "bad interpreter: /usr/bin/env bash^M".
*.sh    text eol=lf
*.bash  text eol=lf

# Windows scripts stay CRLF.
*.bat   text eol=crlf
*.cmd   text eol=crlf
*.ps1   text eol=crlf
